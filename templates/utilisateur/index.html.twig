{% extends 'base.html.twig' %}

{% block title %}Hello UtilisateurController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="container">
    <h1>Gestion des utilisateurs</h1>
    <div class="col">
        <a href="{{ path('test_upload') }}"><h3>Importer utilisateur(s) via csv : </h3></a>
        </div>
    <div id="dtTable" class="mt-3" style=" overflow:auto;"></div>
    <div class="col-2">
        <a href="{{ path('ajout_manuel_utilisateur') }}"> <button class="button btn-primary btn-lg mt-4">Créer un utilisateur </button> </a>        </div>
</div>

<script>
    $( document ).ready(function() {
        $.ajax({
            url:"{{ path('afficher_DtTableUtilisateurs') }}",
            success: function(data) {
                $("#dtTable").html(data);
            }
        });
    });
</script>
{% endblock %}
