{% extends 'base.html.twig' %}

{% block body %}
    <div class="container">
        <div class="row">
            <div class="col s12 m12 l12">
                <h4>Ajout d'une sortie</h4>
            </div>
        </div>

        {{ form_start(form) }}
        <div class="input-field col s12 m12 l12">
            <input type="submit" name="" value="Enregistrer" class="btn btn-primary">
        </div>
        {{ form_end(form) }}
    </div>
{% endblock %}
